services:
  point-mcp:
    image: ${POINT_MCP_IMAGE:-ghcr.io/dariy/point-mcp:main}
    ports:
      - ${DEPLOY_PORT:-8000}:8000
    env_file:
      - path: .env
        required: false
    environment:
      POINT_MCP_TRANSPORT: http
    restart: unless-stopped
